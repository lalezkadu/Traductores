func incremental_forward(number steps)
begin
    for i from 1 to steps do
        forward(i);
    end;
end;

program
    for i from 1 to 360 do
        incremental_forward(i - 2 * true);
        rotater(15);
    end;
end;