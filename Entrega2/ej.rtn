func circle(number radio)
begin
	arc(360, radio);
end;
func plus(number op1, number op2) -> number
begin
	return op1 + op2;
end;
program
	with
		boolean b;
		number n, a;
	do
		x = 9 + 10;
		repeat x times
			forward(50);
			rotatel(90);
		end;
		for x from 1.5 to 4.5 do
            read x;
        end;
	end;
end;